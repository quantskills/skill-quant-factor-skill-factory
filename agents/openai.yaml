interface:
  display_name: "QuantSkills Factor Skill Factory"
  short_description: "QuantSkills workflow for generating and validating factor Skills."
  default_prompt: "Use $skill-quant-factor-skill-factory to generate a new QuantSkills factor Skill batch and validate it on cached real market data."
policy:
  allow_implicit_invocation: true
